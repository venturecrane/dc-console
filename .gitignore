# Dependencies
node_modules/

# Environment
.env
.env.local
.env.*.local
.dev.vars

# Build
.next/
dist/
.wrangler/

# OS
.DS_Store

# IDE
.idea/
.vscode/

# Test
coverage/
playwright-report/
test-results/

# Playwright Clerk auth state (captured session secret — never commit)
playwright/.clerk/

# Vercel
.vercel/

# Infisical
.infisical.json
.vercel

# UI drift audit (CI artifacts, not durable source)
.design/audits/

# Enterprise skill triplet synced from crane-console on every `crane dc` launch
# (see syncVentureSkills in crane-console/packages/crane-mcp/src/cli/launch-lib.ts).
# Source of truth lives in crane-console; tracking here would produce a
# large dirty tree on every canon update.
.claude/commands/
.gemini/commands/
.agents/skills/
